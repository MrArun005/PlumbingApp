{
  "name": "@pipefix/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-cutting primitives: Money (bigint paise), env validation, error taxonomy, zod helpers",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.18.14",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
